<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tgy.rtls.data.mapper.park.floorLock.CarPlateMapper">
  <resultMap id="BaseResultMap" type="com.tgy.rtls.data.entity.park.floorLock.CarPlate">
    <!--@mbg.generated-->
    <!--@Table car_plate-->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="plate_number" jdbcType="VARCHAR" property="plateNumber" />
    <result column="company_id" jdbcType="BIGINT" property="companyId" />
    <result column="company_name" jdbcType="VARCHAR" property="companyName" />
    <result column="map_id" jdbcType="BIGINT" property="mapId" />
    <result column="map_name" jdbcType="VARCHAR" property="mapName" />
    <result column="phone_number" jdbcType="VARCHAR" property="phoneNumber" />
    <result column="create_time" jdbcType="INTEGER" property="createTime" />
    <result column="update_time" jdbcType="INTEGER" property="updateTime" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    c.id,c.plate_number,c.company_id,c.company_name,c.map_id,c.map_name,c.phone_number,c.create_time,c.update_time
  </sql>

  <!--$var desc=1-->
  <select id="getCarPlate" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from car_plate c
    where 1 = 1
    <if test="map != null">
      and map_id = #{map}
    </if>
    <if test="companyId != null and companyId!=''">
      and company_id = #{companyId}
    </if>
    <if test="licensePlate != null and licensePlate != ''">
      and plate_number like concat('%',#{licensePlate},'%')
    </if>
    <if test="phone != null and phone != ''">
      and phone_number like concat('%',#{phone},'%')
    </if>
    <if test="mapids != null and mapids.length != 0">
      and map_id in
      <foreach collection="mapids" item="mapId" close=")" open="(" separator=",">
        #{mapId}
      </foreach>
    </if>
    <if test="desc != null">
      order by ${desc}
    </if>
  </select>

  <select id="getCarPlateById" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from car_plate c
    where 1=1
    <if test="id!=null">and c.id=#{id}</if>
  </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tgy.rtls.data.mapper.park.MonthActiveUserRecordMapper">
  <resultMap id="BaseResultMap" type="com.tgy.rtls.data.entity.park.MonthActiveUserRecord">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="userid" jdbcType="BIGINT" property="userid" />
    <result column="loginTime" jdbcType="TIMESTAMP" property="logintime" />
  </resultMap>
  <sql id="Base_Column_List">
    id, userid, loginTime
  </sql>
  <update id="updateBatch" parameterType="java.util.List">
    update monthActiveUserRecord
    <trim prefix="set" suffixOverrides=",">
      <trim prefix="userid = case" suffix="end,">
        <foreach collection="list" index="index" item="item">
          when id = #{item.id,jdbcType=BIGINT} then #{item.userid,jdbcType=BIGINT}
        </foreach>
      </trim>
      <trim prefix="loginTime = case" suffix="end,">
        <foreach collection="list" index="index" item="item">
          when id = #{item.id,jdbcType=BIGINT} then #{item.logintime,jdbcType=TIMESTAMP}
        </foreach>
      </trim>
    </trim>
    where id in
    <foreach close=")" collection="list" item="item" open="(" separator=", ">
      #{item.id,jdbcType=BIGINT}
    </foreach>
  </update>
  <insert id="batchInsert" parameterType="map">
    insert into monthActiveUserRecord
    (id, userid, loginTime)
    values
    <foreach collection="list" item="item" separator=",">
      (#{item.id,jdbcType=BIGINT}, #{item.userid,jdbcType=BIGINT}, #{item.logintime,jdbcType=TIMESTAMP}
        )
    </foreach>
  </insert>

  <insert id="add">
    insert into monthActiveUserRecord(userid, loginTime)
    values (#{userid},#{logintime})
  </insert>

  <insert id="insertmap">
    insert into map_monthactiveuserrecord(userid,map,loginTime)
    values (#{userid},#{map},#{logintime})
  </insert>
</mapper>